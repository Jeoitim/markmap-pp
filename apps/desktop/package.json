{
  "name": "markmap-plus-plus-desktop",
  "productName": "markmap++",
  "version": "0.1.0",
  "description": "Markdown mind-map workspace with GitHub sync and AI assistance",
  "author": "Jeoitim",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "clean": "del-cli dist out",
    "build:main": "tsc -p tsconfig.json",
    "build:renderer": "cross-env VITE_BASE_PATH=./ VITE_OUT_DIR=../../apps/desktop/dist/renderer pnpm --dir ../../examples/react-example build",
    "build": "pnpm clean && pnpm build:main && pnpm build:renderer",
    "dev:renderer": "pnpm --dir ../../examples/react-example dev --host 127.0.0.1",
    "dev": "pnpm build:main && concurrently --kill-others --success first \"pnpm dev:renderer\" \"wait-on tcp:127.0.0.1:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "package": "pnpm build && electron-forge package",
    "make:win": "pnpm build && electron-forge make --platform=win32 --arch=x64"
  },
  "devDependencies": {
    "@electron-forge/cli": "^7.11.2",
    "@electron-forge/core": "^7.11.2",
    "@electron-forge/maker-squirrel": "^7.11.2",
    "@electron-forge/maker-zip": "^7.11.2",
    "@electron-forge/plugin-fuses": "^7.11.2",
    "@electron/fuses": "^1.8.0",
    "@types/node": "^24.10.1",
    "concurrently": "^10.0.4",
    "cross-env": "^10.1.0",
    "del-cli": "^6.0.0",
    "electron": "^43.3.0",
    "typescript": "~5.9.3",
    "wait-on": "^9.1.0"
  }
}
